import React from "react";

import styles from "./Header.module.scss";

const Header = () => (
  <header>
    <h1 className={styles.title}>Prepare the playlist for your next gig!</h1>
  </header>
);

export default Header;
